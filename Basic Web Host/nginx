create ec2

add inbound rules for security group: HTTP, HTTPS, SSH

run : amazon-linux-extras list or amazon-linux-extras list | grep nginx to see list/current version

sudo yum install nginx1 -y

after updates and install of software, make outbound rule on port 80 on nginx security group point towards itself

sudo service nginx start

sudo chkconfig nginx1 on
